# POSTMAN COLLECTION - DATA FIELD ERRORS

## 📋 Danh sách các lỗi tìm thấy từ việc kiểm tra DTO classes

### 1. AUTH SERVICE ✅ (Đúng hầu hết)
**RegisterRequest fields (CORRECT):**
- email ✅
- password ✅
- roleName/roleId ✅
- fullName ✅
- dateOfBirth ✅
- address ✅
- isStudying ✅

**LoginRequest fields (CORRECT):**
- email ✅
- password ✅

---

### 2. USER SERVICE ❌ (Nhiều lỗi)
**UserRequest (Internal API - Create User):**
```java
// ACTUAL DTO:
- roleId (Long) ✅
- email (String) ✅
- password (String) ✅
- fullName (String) ✅
- dateOfBirth (LocalDate) ✅
- address (String) ✅
- isStudying (Boolean) ✅
```

**Current Postman Error:**
- ❌ Using "roleName" instead of "roleId"
- ❌ Using "role" instead of "roleId"

**Fix Required:** Change all user creation requests to use `roleId` (Long) not `roleName`

---

### 3. QUESTION SERVICE ❌ (Nhiều lỗi)

#### 3.1 FieldRequest (CORRECT)
```java
- name ✅
- description ✅
```

#### 3.2 TopicRequest (CORRECT)
```java
- fieldId ✅
- name ✅
- description ✅
```

#### 3.3 LevelRequest (CORRECT)
```java
- name ✅
- description ✅
- minScore ✅
- maxScore ✅
```

#### 3.4 QuestionTypeRequest (CORRECT)
```java
- name ✅
- description ✅
```

#### 3.5 QuestionRequest ❌ (SAI NHIỀU)
```java
// ACTUAL DTO:
- userId (Long) ✅
- topicId (Long) ✅
- fieldId (Long) ✅
- levelId (Long) ✅
- questionTypeId (Long) ✅
- content (String) ✅  // NOT "questionContent"
- answer (String) ✅  // NOT "questionAnswer"
- language (String) ✅
```

**Current Postman Error:**
- ❌ Using "questionContent" instead of "content"
- ❌ Using "questionAnswer" instead of "answer"

#### 3.6 AnswerRequest ❌ (SAI NHIỀU)
```java
// ACTUAL DTO:
- userId (Long) ✅
- questionId (Long) ✅
- questionTypeId (Long) ✅
- content (String) ✅  // NOT "answerContent"
- isCorrect (Boolean) ✅
- isSampleAnswer (Boolean) ✅
- orderNumber (Integer) ✅
```

**Current Postman Error:**
- ❌ Using "answerContent" instead of "content"

---

### 4. EXAM SERVICE ❌ (Nhiều lỗi)

#### 4.1 ExamRequest ❌
```java
// ACTUAL DTO:
- userId (Long) ✅
- examType (String) ✅
- title (String) ✅
- position (String) ✅
- topics (List<Long>) ✅
- questionTypes (List<Long>) ✅
- questionCount (Integer) ✅
- duration (Integer) ✅
- language (String) ✅
```

**Current Postman Error:**
- ❌ Missing "position" field
- ❌ May have extra fields not in DTO

#### 4.2 ExamQuestionRequest ✅ (CORRECT)
```java
- examId ✅
- questionId ✅
- orderNumber ✅
```

#### 4.3 ResultRequest ✅ (CORRECT)
```java
- examId ✅
- userId ✅
- score ✅
- passStatus ✅
- feedback ✅
```

#### 4.4 UserAnswerRequest ❌
```java
// ACTUAL DTO:
- examId (Long) ✅
- questionId (Long) ✅
- userId (Long) ✅
- answerContent (String) ✅  // This one IS "answerContent"
- isCorrect (Boolean) ✅
```

#### 4.5 ExamRegistrationRequest ✅ (CORRECT)
```java
- examId ✅
- userId ✅
- registrationStatus ✅
```

---

### 5. CAREER SERVICE ✅ (CORRECT)
**CareerPreferenceRequest:**
```java
- userId ✅
- fieldId ✅
- topicId ✅
```

---

### 6. NEWS SERVICE ❌ (Nhiều lỗi)
**NewsRequest:**
```java
// ACTUAL DTO:
- userId (Long) ✅
- title (String) ✅
- content (String) ✅
- fieldId (Long) ✅
- examId (Long) ✅
- newsType (String) ✅
- companyName (String) ✅
- location (String) ✅
- salary (String) ✅
- experience (String) ✅
- position (String) ✅
- workingHours (String) ✅
- deadline (String) ✅
- applicationMethod (String) ✅
```

**Current Postman Error:**
- ❌ Using "company" instead of "companyName"
- ❌ Using "requirements" (array) - NOT IN DTO
- ❌ Using "benefits" (array) - NOT IN DTO

---

### 7. RECRUITMENT SERVICE ❌
**Same as News Service** - uses NewsRequest DTO

---

### 8. NLP SERVICE ⚠️ (Cần kiểm tra Python code)
Cần đọc FastAPI endpoint definitions để xác nhận chính xác các fields

---

## 🔧 PRIORITY FIXES NEEDED:

### Critical (Must Fix):
1. **QuestionRequest**: Change `questionContent` → `content`, `questionAnswer` → `answer`
2. **AnswerRequest**: Change `answerContent` → `content`
3. **UserRequest**: Change `roleName` → `roleId` (Long)
4. **NewsRequest**: Change `company` → `companyName`, remove `requirements`/`benefits` arrays
5. **ExamRequest**: Add `position` field

### Medium Priority:
- Review all sample data to match DTO types (Long vs String, Boolean vs boolean)
- Check all LocalDate formats (should be "YYYY-MM-DD")

### Low Priority:
- Verify optional vs required fields match validation annotations
- Check enum values match entity definitions

---

## 📝 Next Steps:

1. ✅ Read all DTO files (DONE)
2. ⏳ Create corrected Postman collections
3. ⏳ Verify with swagger-ui.html
4. ⏳ Test with running services
5. ⏳ Update documentation

---

**Generated:** From actual DTO class analysis
**Status:** Ready for correction
